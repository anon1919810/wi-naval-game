# 玩法原型交接 · 2026-09-19

> 入口：本文件 + `queen_mary_v3/GAMEPLAY_LAB.md` + `docs/compose/spec/combat-vfx.md`  
> 仓库：本地 `C:\Users\杨睿\Desktop\HMS_Queen_Mary_建模成果_2026-09-17`  
> 远端：`https://github.com/anon1919810/wi-naval-game`（**`main`**）  
> Unity：`D:/Unity/Projects/QueenMaryNaval/QueenMaryNaval` · 2022.3.62f3c1 · URP 14

---

## 1. 当前产品状态（一句话）

一战战列巡洋舰 **HMS Queen Mary** 灰盒资产 + **Unity GameplayLab**：可第三人称瞄准、四塔齐射（射界门控）、穿深/进水、同模靶船、战斗 VFX。仍是**实验室**，不是完整拟真海战。

| 层 | 状态 |
|---|---|
| 资产管线 | 型值表驱动 Blender 灰盒；契约 JSON；LOD 双剖面；`historically_certified:false` |
| 运行时数据 | 14 舱室、装甲/穿深表、firing_arcs、lod_profiles |
| 玩法壳 | 炷台选择可选；鼠标世界方位群炮；第三人称；光标锁定 |
| 战斗 | 命中→舱室进水→系统状态；estimate 穿深曲线 |
| 特效 | 炮口/细曳光/水花/爆炸；无商店海战材质包 |

---

## 2. 如何跑 GameplayLab

```text
1. Unity 打开 D:/Unity/Projects/QueenMaryNaval/QueenMaryNaval
2. Console 无编译错误（脚本在 Assets/Scripts/Naval + Assets/Editor）
3. Tools → Naval → Build gameplay lab   # 场景含玩家舰+靶船更稳
4. 打开 Assets/Scenes/GameplayLab.unity → Play
```

未重建场景时：Play 后 `ShipLabRuntime` 会在 `(60,0,520)` 自动补同模靶船。

### 操作

| 键 | 作用 |
|---|---|
| 鼠标左右 | 视角绕船心 + 四塔同一罗经方位 |
| 鼠标上下 | 俯仰 |
| Space / 左键 | 齐射；射界盲区塔 SKIP（无特效） |
| Esc / 点 Game 窗 | 释放 / 锁定光标（必须锁定才有连续鼠标） |
| Tab | 第三人称 → 战术 → 舰队 |
| R / T / F1 | 重置双船 / 靶船漂移 / 调试 HUD |

准星：细十字；左上：方位/炮塔可射/开火/命中；右上：我方+靶船。

---

## 3. 目录与代码地图

| 路径 | 用途 |
|---|---|
| `queen_mary_v3/queen_mary.py` | v3 Blender 生成脚本（改模型后 `rebuild.ps1`） |
| `queen_mary_v4/` | **v4 模块化精修包**（外观+内部 FBX、贴图、剖视；未接入 Lab） |
| `queen_mary_v3/unity/` | **唯一有效** Unity 源码（勿同步 `docs/legacy_codex_unity`） |
| `lod_profiles.json` | 战术/舰队 LOD 阈值 |
| `penetration_main.json` / `armour_zones.json` | 穿深曲线与装甲区 |
| `firing_arcs*.json` | 射界（command=worldYaw−rest） |
| `GAMEPLAY_LAB.md` | 玩法操作与验收 |
| `docs/compose/spec/` | compose 特性文档 |
| `research/` | 型线/LOD/特效调研（部分在仓库根） |

### 运行时脚本（Naval）

`ShipContract` · `ShipDefinition` · `ShipCompartment` · `ShipFloatPrototype` · `ShipSystemsState` · `ShipTurretController` · `ShipFiringArcs` · `ShipMouseGroupAim` · `ShipCameraRig` · `ShipAimUI` · `ShipGunBattery` · `ShipPenetration` · `ShipTargetShip` · `ShipCombatVfx` · `ShipLabRuntime`

### Editor 批处理

`ShipRuntimeBuilder.BuildBatch` · `ShipGameplayLabBuilder.BuildBatch` · `ShipDeliveryChecks` · `ShipRuntimeAcceptance` · `ShipBenchmarkBuilder`  
参数：`-lodProfile <id>` 可选。

---

## 4. 硬约定（改动前必读）

1. **数据驱动**：Unity 只读契约/JSON，不手抄尺度与模块名。  
2. **坐标**：Unity +X 右舷、+Y 上、+Z 艏；炮塔 command 为 rest 叠加；yaw 世界方位 0°=舰艏。  
3. **射界**：`yawCommand = worldAimYaw − rest_yaw`；盲区 SKIP，不硬编码“谁不能打”。  
4. **LOD**：生效阈值 = `lod_profiles` 作者值 × `QualitySettings.lodBias`；`LODGroup.size` 用实测（本资产≈213.4）。  
5. **史实**：`historically_certified:false`；估计值标 estimate/source。  
6. **不要**合并炮塔链进静态 LOD0/1/2；装饰对象预算≤10、可渲染网格≤65。  
7. **推送**：系统 Git `C:/Program Files/Git/cmd/git.exe` + SSH 443 + `id_ed25519_anno`；勿用坏 SSL 的 PortableGit push。

---

## 5. 本轮（2026-09-19）已交付

| 项 | 分支/提交 |
|---|---|
| T9 穿深表 + 系统状态 | 已并 master（此前） |
| T10 操作壳 / T11 靶船 / T12 鼠标群炮+光标 | master `63d6427` 一带合入 |
| UI 清理 + 弹道对齐世界方位 | `adb81b9` |
| 编译修复 commandHint | `8613c99` |
| T16 战斗 VFX | `feature/t16-combat-vfx`：`96d86e2`…`baaedf9`（**随本次 merge 进 master**） |

远端推送目标：`wi-naval-game` → `main` = 合并后 master。

---

## 6. 验收与证据

| 证据 | 说明 |
|---|---|
| `verify_penetration.py` | 穿深表离线断言 |
| `verify_firing_arcs_runtime.py` | 射界盲区断言 |
| `runtime_acceptance/ship_runtime_build.json` | LOD 剖面/舱室/碰撞构建报告 |
| GameplayLab Play | 玩家可打靶船；HUD 显示命中与系统 |

**限制**：浮态近似；穿深 estimate；无完整 AI/损管；帧时间引用档位顺序勿引用单次 ms；特效为可读性壳非认证弹道。

---

## 7. 已知问题 / 下一步

| 优先级 | 项 |
|---|---|
| P0 | 有空跑一次 **Build gameplay lab**；`fleet_1_2km` 剖面实机验收 |
| P1 | 装填 CD；命中反馈再强化；靶船简单机动 |
| P1 | T8 史料：型线图/1913 照片 → `hull_offsets.json` |
| P2 | 第二艘船骨架（复用契约管线） |
| P2 | 真实海面资产评估（商店包，另任务） |
| 卫生 | `research/` 若未入库可后续 commit；大 zip 可考虑 Git LFS |

---

## 8. 复现命令（资产侧）

```powershell
cd "C:\Users\杨睿\Desktop\HMS_Queen_Mary_建模成果_2026-09-17\queen_mary_v3"
.\rebuild.ps1
# Unity 编辑器关闭时：
.\unity\verify_runtime.ps1 -ProjectPath "D:/Unity/Projects/QueenMaryNaval/QueenMaryNaval" -Benchmark
```

推送：

```powershell
& "C:/Program Files/Git/cmd/git.exe" -C "<仓库根>" push ssh master:main
```

---

## 9. 给下一任的三条

1. **Play 前先确认场景/脚本已编译**；特效异常先看 HUD 是否 `SKIP` 再查 VFX 预算。  
2. **不要**为了“好看”先重做高模——战斗可读性优先特效与反馈。  
3. **史实结论**必须有 source 或标 estimate；改完重建并记录 `geometry_sha256`。
