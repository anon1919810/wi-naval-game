# Queen Mary · LOD 精修与剖面补全 · 2026-09-18

> 本文件是 **feature/model-refine-lod** 的当前验收说明。  
> `运行时验收_2026-09-18.md` 与 `复核_2026-09-18.md` 描述的是**上一基线**（几何 `5b248d11…`、阈值默认 `0.20/0.08/0.03/0.01`），仍作历史对照，**不是本分支现状**。

## 当前几何

| 项 | 值 |
|---|---|
| `geometry_sha256` | `9d6d6a765993487ef4e0112ea217cad8e48642a51dab8482a31e2a36189df7c0` |
| 上一基线 | `5b248d11c70438dbc2cfbb33e30ffce4d3d346714dc8ee3390d948e4d37a4037` |
| 对象 / 网格 | 101 / 85（装饰合并预算未破：装饰角色对象 ≤10，可渲染网格 63≤65） |
| Blender 验证 | `rebuild.ps1 -SkipRender` 通过：32 geometry + 13 assumption + 18 FBX |
| 重复生成 | `repeat_generation_passed: true` |

### 可见层精修（均不升格为史实认证）

- Funnel_2 截面 `3.55×3.55` 圆形，识别环烘进网格；属性 `recognition_*`（source: worldwar1 + estimate）。
- 舰桥/艉部中距块面带并入 `Deck_Visual_Details`（estimate）。
- pole mast、防雷网在位、无 AA、四炮塔链独立 —— 1913 口径未破。
- 禁止项未引入：Tiger 布局、1914+ AA、三脚桅、型线重写。

## LOD 配置

| 项 | 值 |
|---|---|
| 配置文件 | `queen_mary_v3/lod_profiles.json` |
| 默认剖面 | `tactical_50_200m` → `0.45 / 0.18 / 0.06 / 0.02`，`recommendedLodBias=1` |
| 舰队剖面 | `fleet_1_2km` → `0.40 / 0.18 / 0.07 / 0.025` |
| 公式 | `relH = lodGroupSize * lodBias / (2 * d * tan(fov/2))` |
| 实测 `LODGroup.size` | **213.40 m**（RecalculateBounds 后最长包围轴 = 舰长；标定必须用实测值） |

### Unity 运行时构建（真机）

入口：`Naval.EditorTools.ShipRuntimeBuilder.BuildBatch`  
证据：`runtime_acceptance/ship_runtime_build.json`

| 字段 | 值 |
|---|---|
| status | passed |
| lodProfileId | tactical_50_200m |
| lodScreenHeights | 0.45 / 0.18 / 0.06 / 0.02 |
| lodRecommendedBias | 1.0 |
| lodGroupSizeMeasured | 213.40 |
| 关渲染 / 碰撞体 / 舱室 | 24 / 25 / 14 |
| LOD0→3 三角形 | 30,088 / 30,088 / 17,144 / 15,288 |

批处理可用 `-lodProfile <id>` 切换剖面（例如 `fleet_1_2km`）；省略则用 JSON 的 `default_profile`。

## 离线 LOD 数学检查

```powershell
# 在 queen_mary_v3 下
$env:MIMO_PYTHON -m ...  # 或系统 python
python .\verify_lod_profiles.py
```

输出见 `runtime_acceptance/verify_lod_profiles.log`：`lod_profile_math_ok`。  
按实测 size=213.4 与**质量档 lodBias=2** 叠加后，现舰队相机 automatic 实测为全 LOD1（见上表）；`fleet_1_2km` + recommendedLodBias=1 在 1700 m 期望 LOD2（尚未本机重建验证该剖面）。

## 舰队基准（新代码重跑）

证据：`runtime_acceptance/fleet_benchmark_lod_profile_20260918.json`（并覆盖 `benchmark/`、`fleet_benchmark/fleet_benchmark.json`）。

| 字段 | 值 |
|---|---|
| status | completed |
| lodProfileId | tactical_50_200m |
| lodScreenHeights | 0.45 / 0.18 / 0.06 / 0.02 |
| lodRecommendedBias | 1.0 |
| lodGroupSizeUsed | 213.40 |
| quality lodBias | 2.0（质量档 5；与剖面 recommendedLodBias=1 不同，报告已记录） |
| automatic | 15 艘全部落在 **LOD1**（relH 0.237–0.322；未达作者阈值 0.45，≥0.18） |
| forced_LOD0/1/2/3 中位 ms（仅顺序参考） | 2.43 / 1.90 / 1.80 / 1.39 |

automatic notes 含剖面、实测 size、公式、掉入下一档距离约 2542 m。  
新战术剖面下自动档不再是全 LOD0，而是全 LOD1（编队纵深仍导致同档）；LOD 收益看 forced 档三角形/子网格削减，不引用单次 ms。

## 尚未完成 / 限制

- 帧时间仍只引用**档位顺序与三角形削减**，不引用单次 ms。
- 型线仍是 `builtin_estimate`；精修不替代史实认证。
- 本轮未再跑 `fleet_1_2km` 运行时重建；批处理已支持 `-lodProfile fleet_1_2km`。
- `runtime_delivery_manifest.json` / `integration_verification.json` 为历史哈希注记，**以本文件 + `build_result.json` + `ship_runtime_build.json` + `fleet_benchmark_lod_profile_20260918.json` 为准**。

## 研究与规格

- 研究报告（仓库根，不在 worktree 内）：`C:\Users\杨睿\Desktop\HMS_Queen_Mary_建模成果_2026-09-17\research\queen-mary-refine-lod\REPORT.md`
- 规格：`docs/compose/spec/model-refine-lod.md`
