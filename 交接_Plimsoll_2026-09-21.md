# 交接 · Plimsoll（自研舰船设计计算器）· 2026-09-21

> **本轮范围：只做 Plimsoll。** 游戏侧接入（`ShipFloatPrototype` 换数、
> `hydrostatics.json` 写回、Unity 验收）**不在此轮**，待 Plimsoll 自身稳定后另立项。
> 前序游戏侧交接见 `交接_3D协作与试验场_2026-09-20.md`。
>
> **当前基线：`442ed0f`** · 测试 **79 项全绿** · 工作区干净

---

## 1. 一句话

Plimsoll 是一个**可脚本化、可验证、可溯源**的舰船静水力与大角稳性计算核心。
它要解决的是 SpringSharp 的三个硬伤：**没有真实船体几何、没有大角稳性、不能脚本调用**。

与 SpringSharp **无任何代码关系**（其界面只作需求覆盖度检查表，参考图在 `docs/sps-reference/`）。

---

## 2. 资源地址（必读）

| 类型 | 地址 |
|---|---|
| 仓库根 | `C:\Users\杨睿\Desktop\HMS_Queen_Mary_建模成果_2026-09-17` |
| Python | `C:\Users\杨睿\.workbuddy\binaries\python\versions\3.13.12\python.exe`（纯标准库，**无 numpy**） |
| 核心代码 | `tools/plimsoll/` |
| 规格 | `docs/sps-replacement/SPEC.md` |
| **改进计划（含「如何续接」）** | `docs/sps-replacement/PLAN.md` ← **新会话先读这个** |
| 型线图资源调研 | `docs/sps-replacement/型线图资源.md` |
| 装甲验收标准数据 | `data/british_armour_standards_1908_1912.json`（+ 原始扫描件 `data/armour_standards_source/`） |
| SpringSharp 界面参考图 | `docs/sps-reference/00–06_*.png` |
| 系统 Git（网络操作用） | `C:\Program Files\Git\cmd\git.exe`（PortableGit 的 SSL 是坏的） |

---

## 3. 当前状态

### 已完成的两层

| 层 | 文件 | 吃什么 | 给什么 |
|---|---|---|---|
| **L0 参数化** | `hydrostatics.py` | 长宽吃水 + 系数 | 排水量、Awp、TPC、KB、BM、KM、GM、横摇周期、MCT1cm |
| **L1 几何法** | `geometry.py` + `geometric.py` | **站位剖面** | 真实静水力 + **大角稳性 GZ 曲线** |
| 型线导入 | `offsets.py` | 型值表 / `hull_offsets.json` | 站位剖面 |
| 入口 | `cli.py` | `ship.json` | 表格 + `result.json` |

### 实数（Queen Mary 案例，LWL 212.8 m / Cb 0.533 / KG 8.6 为 estimate）

**L0 参数化**
排水 26,780 t（对输入 26,770 偏差 **+0.04%**）· Awp 4,613.5 m² · TPC 47.29 t/cm ·
KB 5.196 · BM_T 8.215 · KM 13.410 · **GM 4.810 m** · 横摇 9.42 s · BM_L 511.8 · MCT1cm 644

**L1 型值表船体**（141 站位，设计水线 z=0 = 吃水 9.9 m）
体积 30,943.9 m³ = **31,717 t** · Awp 4,628.4 · KB 6.005 · BM_T 7.272 · KM 13.277 ·
**GM 4.677 m** · GZ：10° 0.805 / 20° 1.559 / 30° 2.063 / **40° 2.116（峰值）**

### 已建立的三条交叉佐证

1. **模型体积特性对得上实船**：型值表船体在满载吃水积分得 31,717 t，
   对实船满载 31,650 t 差 **+0.21%**；模型 Cb 0.5405 vs 实船 0.5408，差 **0.06%**。
   → 该型值表虽自述"形状是估算"，但**体积特性可信**。
2. **与项目现有常数一致**：游戏里 `waterplaneAreaM2 = 4610.4`，
   L0 算出 4,613.5（差 **0.07%**）、L1 算出 4,628.4（差 0.39%）。
3. **与 v3 静水力记录一致**：VCB（相对水线）本库 −3.895，仓库记录 −3.89，差 **0.13%**
   —— 这是提取正确性最有力的证据（形状敏感量，与求积粗细无关）。

### 验证方式（三层，必须保持）

1. **解析解**：方箱 —— 静水力逐位吻合；GZ 与闭式解
   `GM·sinφ + (B²/24T)·sin³φ/cos²φ` 吻合。另有**整体下移的方箱**专门验坐标基准。
2. **L0 ↔ L1 交叉验证**：同一形状假设下两条独立路径必须一致（水线面派生量差 <0.1%）。
3. **数值性质**：离散收敛性、等体积守恒、初始斜率 → GM、纵倾闭式解。

---

## 4. 怎么跑

```bash
PY="C:/Users/杨睿/.workbuddy/binaries/python/versions/3.13.12/python.exe"

# 主案例：L0 + GZ（默认用型值表船体，会声明来源）
"$PY" tools/plimsoll/cli.py tools/plimsoll/cases/queen_mary_1913.json --gz -o out.json

# 换合成参照船体（与 L0 同源，仅供交叉验证）
"$PY" tools/plimsoll/cli.py tools/plimsoll/cases/queen_mary_1913.json --gz --hull reference

# 三套测试（79 项）
"$PY" tools/plimsoll/tests/test_hydrostatics.py     # 28
"$PY" tools/plimsoll/tests/test_geometric.py        # 35
"$PY" tools/plimsoll/tests/test_offsets_import.py   # 16
```

---

## 5. 关键设计决定（为什么这么做）

**① 一参数水线面形状模型（L0）**
`f(x) = (1−(2x/L)²)^p`，各阶矩用 Γ 函数**解析**求出。给定 `Cwp` 反解 `p` 后其余量唯一确定
—— **没有"再挑几个系数"的自由度**。`p→0` 退化为方箱，此时
`KB=T/2`、`BM_T=B²/(12T)`、`I_L=B·L³/12`，单测直接断言这组闭式解。
**模型自洽性由解析值保证，不靠调参调对了。**

**② 倾斜用同一套裁剪（L1）**
浮态是一张水线平面 `z = x·tanθ + y·tanφ + d`。**固定 x 时水线在剖面内仍是直线**，
与纯横倾只差截距 —— 所以横倾/纵倾/两者叠加共用同一套 Sutherland–Hodgman 裁剪，
不是三种算法。**θ > 0 = 艏倾**（已写进 docstring）。

**③ GZ 用等体积倾斜**
每个横倾角下**重解平衡水线**，再按 `GZ = y_B·cosφ + (z_B − KG)·sinφ`（z_B 与 KG 都自龙骨量）
求复原力臂。这是 SpringSharp 完全没有的能力。

**④ 核心三条纪律**
纯函数（`dict → dict`，不 import GUI/Unity/网络）· **不做四舍五入**（取整只在显示层）·
每个输出项带 `formula` / `source` / `estimate`。

**⑤ 两个自由度分开收口**
`solve_waterline` 一维求 d（阶段 2.1 已支持给定纵倾）；
**纵倾平衡（解出 θ 本身）是二维求根，属阶段 2.2** —— 不要混进同一个函数。

---

## 6. 已知限界与未决（**不要越级引用**）

| 项 | 状态 |
|---|---|
| **型线是估算的** | 生成脚本自述"三个主尺度是给定的，**之间的形状是估算**"。体积特性已验证（0.21%），但**形状分布（KB/BM/GZ）未经外部验证** |
| 真实型线图 | 未获得。书（John Roberts《Battlecruisers》）国内不好买；NMM 未见本舰完整型线图。**接入点已就位**：放一个 `hull_offsets.json` 即可，代码不用改 |
| **甲板浸没角 20.6°** | 干舷 5.10 ÷ 半宽 13.60。超过它浸没剖面被主甲板截断、GZ 偏小 → **该角以上数值不应引用**（CLI 会自动提示） |
| `waterplane_coeff = 0.80` | estimate（但型值表隐含 0.7994，吻合 0.08%） |
| `kg_m = 8.6` | estimate，属 L2 重量分组。GM 与之强耦合，故输出附 **KG 敏感性表** |
| 吨位单位 | 史料长吨/公吨未注明，来源间有 26770/26780、31650/31486 的正常出入，各自标注即可 |
| 纵倾平衡 | **未做**（阶段 2.2） |
| 自由液面修正 | **未做**（阶段 3） |
| 破损稳性 | **未做**（阶段 4，是阶段性目标） |
| 闭合体积三值不一致 | 本方法 55,234 / 本方法多面体精确值 54,226 / 网格 54,865.7。已排除剖面密度、站位间距、插值复现，**根因未查明**；该项在水线以上，不影响浮态。阶段 6.1 |

---

## 7. 踩过的坑（**别重踩**）

1. **两套坐标基准并存时，任何跨基准的量必须显式换算。**
   型值表船体：龙骨 z=−9.9、设计水线 z=0；合成参照船体：龙骨 z=0、水线 z=T。
   KB/KG 自龙骨量，积分出的 z_B 相对各自原点 —— 混用会让 GM 变成负数。
   **同一晚踩了两次**（KB 为负、甲板浸没角算错），且**合成船体掩盖了第一次**。
   → 现由 `keel_z()` / `top_z()` 统一换算，测试有 `TestDatumShift` 专门盯。
2. **选来源要看它在多源中的位置，孤例必须存疑。**
   我把 LWL 取成 Tyne 的 675 ft（孤例），其余五源聚在 213–214 m ——
   还连累 `Cb = 0.551` 成了从错误 LWL 反推的**循环论证**。
   改对 LWL（212.8）+ Cb（0.533）后排水量偏差才回到 +0.04%。
   **两个量必须同步改**，`test_displacement_matches_input` 是这条的护栏。
3. **外部摘要里的具体数字必须回源核对。**
   照搬调研摘要说"平行中体恰好 13.60 m"，回查原表发现 y=+30 处已收到 13.55。
4. **最直觉的那条断言，往往正是抓不住 bug 的那条。** 两次实证：
   · `test_initial_slope_matches_gm` 里 keel_z 等式两边同号相消 → **假绿**；
   · 纵倾去掉后**体积仍是 5000.0000**（tanθ 项在对称区间积掉）→ 只验体积测不出。
   → 断言必须**锚在源数据的绝对值**上（如"龙骨 == 型值表最小值 −9.90"），
     光靠"数值落在合理区间"过滤不掉"差一点"。
5. **环境**：沙箱拦 `cmd.exe`/`wmic`/`schtasks` 及文本里那个脚本宿主的名字；
   bash 的 `rm` 被 safe-delete 拦（删工程 Temp 要先杀进程再 `mv`）；
   Unity 跑完 `-runTests` 进程可能不退出、占着 `Temp/UnityLockfile`（后续 batchmode exit 21）。

---

## 8. 下一步

**阶段 2.2 纵倾平衡求解**：给定排水量与 LCB(=LCG)，解出 `(d, θ)` 使**体积与纵向力矩
同时平衡** —— 二维求根。`integrate()` 已返回 `xlcb`。
做法：内层解 d 使体积达标、外层解 θ 使 xlcb 对齐 LCG（或 Newton 法），给 θ 设上下界。
验收：方箱解析解 + 一个已知纵倾的案例，迭代收敛稳定。

之后：**自由液面修正（阶段 3）→ 破损稳性（阶段 4，阶段性目标）**。
完整清单见 `PLAN.md`。

---

## 9. 证据索引

| 证据 | 位置 |
|---|---|
| 改进计划与勾选状态 | `docs/sps-replacement/PLAN.md` |
| 规格与架构原则 | `docs/sps-replacement/SPEC.md` |
| 案例输入（每个数值带来源） | `tools/plimsoll/cases/queen_mary_1913.json` |
| 案例输出（含 trace 与 GZ） | `tools/plimsoll/cases/queen_mary_1913.result.json` |
| 型线图资源调研 | `docs/sps-replacement/型线图资源.md` |
| 装甲验收标准（穿深模型的外部锚点） | `data/british_armour_standards_1908_1912.json` |
| 每次改动的理由 | `git log`（提交信息里写了"为什么"，不只"改了什么"） |

---

## 10. 给下一任

1. **先读 `PLAN.md` 顶部的「如何续接」** —— 那里有要读的四份文档与下一个未勾选项。
2. **不要相信"测试全绿"** —— 先做一次变异验证（把关键量改坏，看测试红不红）。
   本主题已经被假绿坑过两次。
3. **改数值代码要有可检查的停顿点**，不要让无人监督的进程自己提交。
   （后台子代理适合**调研与独立复核**，不适合写数值实现。）
4. **交接与限界随代码一起提交**：本轮所有结论都写进了 README / PLAN / 案例 JSON /
   提交信息 —— 请保持这个习惯，否则下一个会话要重学一遍。
