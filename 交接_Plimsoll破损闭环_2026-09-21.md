# 交接 · Plimsoll 长任务（破损稳性闭环）· 2026-09-21

> 用户离开期间执行完毕。范围：PLAN **4.2–4.4** + **5.1/5.2/5.4**。
> master 基线：`cc266fe`（2.2+FSC+4.1 已推送）。

## 1. 一句话

破损后：解浮态 → 剩余 GZ → 场景落盘；FSC **只计一次**；一键回归 **129 全绿**。

## 2. 分支与状态

| 项 | 值 |
|---|---|
| 分支 | `feature/plimsoll-damage-loop` |
| 基线 | `master` / `ssh/main` = `cc266fe` |
| 实现 | `0f48727` + critical 修复与 finalize 提交 |
| 工作区 | `.worktrees/plimsoll-damage-loop` |
| Spec | `docs/compose/spec/plimsoll-damage-loop.md`（delivered） |

## 3. API

```text
damage.solve_flooded_equilibrium(hull, ship, tanks, rho=...)
damage.remaining_gz_curve(hull, kg_eff, V*, free_surface_tanks=None, fsc_already_in_kg=...)
damage.flood_tanks_to_fs_tanks(tanks)   # flood_fraction → fill_fraction
damage.run_damage_scenario(hull, scenario)
tools/plimsoll/run_damage_scenarios.py
tools/plimsoll/run_all_tests.py         # PLIMSOLL_REGRESSION
tools/plimsoll/__init__.py              # import plimsoll; plimsoll.damage....
integrate()["trace"]
```

## 4. 验证

- damage_loop **11/11**
- `run_all_tests.py` → **run=129 fail=0**
- 场景 single/double/engine → `cases/out/*.result.json`，stable=True
- 变异与 review critical 已闭环

## 5. 硬限界

1. FSC **只计一次**（kg_eff 路径禁止再传 tanks；传 tanks 必须显式 `fsc_already_in_kg=False`）
2. `flood_fraction`≠`fill_fraction`，勿混用
3. KM=upright(d) estimate；横倾仅小角度
4. 舱室 L×b 显式；型线仍估算
5. 5.3 sweep 未做

## 6. 回来后

```bash
PY="C:/Users/杨睿/.workbuddy/binaries/python/versions/3.13.12/python.exe"
"$PY" ".worktrees\plimsoll-damage-loop\tools\plimsoll\run_all_tests.py"
# 确认后：checkout master && merge feature/plimsoll-damage-loop && push ssh master:main
```

下一步候选：5.3 · 7.1 穿深校准 · 真实型线 · 游戏侧另立项。
