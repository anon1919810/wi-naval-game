# Queen Mary 损伤试验场交接 · 2026-09-20

> 本文交接的是“可信计算＋清楚剖视”的小型损伤试验场。它用于验证穿透、引信、爆炸代理、破片权重、模块损伤和可解释回放，尚不是完整战斗系统，也不是 Queen Mary 的历史装甲认证。

## 1. 当前基线

| 项目 | 当前值 |
|---|---|
| 资产仓库 | `C:\Users\杨睿\Desktop\HMS_Queen_Mary_建模成果_2026-09-17` |
| Unity 工程 | `D:\Unity\Projects\QueenMaryNaval\QueenMaryNaval` |
| Unity / URP | Unity `2022.3.62f3c1` / URP `14.0.12` |
| Git 基线 | `77c7ab1`，`master` 与 `ssh/main` 指向该提交 |
| 本次改动状态 | 尚未提交；损伤试验场相关文件在工作区显示为未跟踪 |
| 试验场场景 | `Assets/Scenes/DamageRange.unity` |
| 历史认证 | `historically_certified: false` |

源码的唯一有效副本仍放在资产仓库的 `queen_mary_v3\unity\`。Unity 工程 `Assets` 是当前编辑器实际导入副本；修改后要同步两边，避免下一次构建覆盖改动。

## 2. 本次交付内容

### Unity 运行时脚本

- `queen_mary_v3\unity\Scripts\DamageRangeModel.cs`：纯数据和求解逻辑，不依赖 `MonoBehaviour`，便于测试和以后接入主战斗系统。
- `queen_mary_v3\unity\Scripts\DamageRangeView.cs`：生成剖段几何、实验视图、参数面板、炮弹回放和 JSON 导出。
- `DamageRangeView` 使用 `[ExecuteAlways]`，打开场景时生成编辑器预览；Play 后才运行交互和回放。
- 预览根节点名为 `Experimental_Hull_Section` 和 `Report_Visuals_Only`。

### Unity 编辑器脚本

- `queen_mary_v3\unity\Editor\DamageRangeBuilder.cs`
  - `Tools → Naval → Open damage range`：打开已有场景。
  - `Tools → Naval → Build damage range`：重新生成相机、灯光、试验根节点并保存场景。
  - 批处理入口：`Naval.EditorTools.DamageRangeBuilder.BuildBatch`。
- `queen_mary_v3\unity\Editor\DamageRangeOpenOnLoad.cs`
  - 使用 Unity 2022 可用的 `EditorApplication.ExecuteMenuItem`。
  - 当前编辑器会话第一次加载脚本时，尝试打开试验场；如果用户仍在其他场景，手动执行菜单项即可。

### 测试和证据

- `queen_mary_v3\unity\Tests\EditMode\DamageRangeTests.cs`
- `queen_mary_v3\damage_range\RangeChecks.cs`
- `queen_mary_v3\damage_range\RunNunit.cs`
- `queen_mary_v3\damage_range\solver_verification.json`
- `queen_mary_v3\damage_range\README_损伤试验场.md`

## 3. 如何打开和使用

1. 启动 `D:\Unity\Editor\2022.3.62f3c1\Editor\Unity.exe`，打开项目 `D:\Unity\Projects\QueenMaryNaval\QueenMaryNaval`。
2. 如果当前仍是 `GameplayLab`，点击 `Tools → Naval → Open damage range`。
3. 打开 `Assets/Scenes/DamageRange.unity` 后，Scene 视图应出现灰盒剖段；编辑器预览不需要先点击 Play。
4. 点击 Play 才启用完整实验界面。左侧调整参数后点击 `FIRE / RECOMPUTE`。
5. 右侧可以切换剖视、破片路径、回放、逐事件和 `LOAD LAST SHOT`。
6. `EXPORT SHOT JSON` 会保存 `last_shot.json` 和带 UTC 时间戳的报告。默认目录是 Unity 的 `Application.persistentDataPath/DamageRange`；批处理可用 `-labOutput <目录>` 覆盖。

快捷操作：右键拖动旋转视角，Space 暂停，`Baseline` 恢复默认，`Thick plate` 演示厚板阻挡，`Open walls` 清空三层阻力，`Repeat shot` 使用同一随机种子，`Next seed` 改变种子后再开火。

## 4. 计算模型

求解器模型标识为 `experimental-resistance-v1`，报告 schema 为 `damage-range-1`。

### 4.1 弹丸和装甲层

试验剖段沿 x 轴排列：

| 层 | x | 默认厚度 |
|---|---:|---:|
| `Outer_Belt` | 0 m | 180 mm |
| `Inner_Bulkhead` | 3 m | 25 mm |
| `Rear_Bulkhead` | 7 m | 40 mm |

弹丸从 x=-8 m 发射，默认速度 600 m/s，向 x=13 m 的试验边界推进。入射角改变 y 分量，装甲法向分量使用 `abs(dir.x)`。

```text
budget = referencePenMm × speed² / 600²
effectiveThickness = plateMm × resistance / max(abs(dir.x), 0.05)
remainingBudget = max(0, budget - effectiveThickness)
newSpeed = initialSpeed × sqrt(remainingBudget / initialBudget)
```

这是速度平方缩放的可解释阻力代理，不是 WWI 穿甲公式，也没有经过弹种、板材、倾角、弹尖和跳弹史料校准。

板是有限的 8 × 14 m 平面。板厚参与阻力，但当前不计算穿板时间、板材变形、裂纹、装甲崩落或连续破口。视觉板厚最低显示 40 mm，避免薄板在剖视中消失。

### 4.2 引信和事件

`fuseMode`：

- `0 Contact`：第一次碰到配置板即起爆，包括零阻力板。
- `1 Delay`：第一次碰板后按 `fuseDelay` 延迟起爆；弹丸停止后已启动的引信仍会到时起爆。
- `2 Dud`：不引爆。

报告记录 `launch`、`penetrated`、`stopped`、`contact`、`detonation`、`equipment` 和 `exit` 事件。事件时间单调递增，回放只解释事件顺序，不重新进行实时物理求解。

### 4.3 模块损伤

当前模块：`Boiler_Room_1`、`Engine_Room_1`、`Feed_Pump`。

射线路径穿过设备盒时增加固定直接损伤 `0.45`。设备本体暂时不消耗炮弹速度；该近似会在事件说明中标出。

```text
damage = clamp(direct + blast + fragment, 0, 1)
0.15 起 damaged
0.75 起 disabled
```

这三个模块是试验场契约，不应直接当作 Queen Mary 的最终舱室拓扑。`Feed_Pump` 是实验用 ID，尚未绑定主战斗系统契约。

### 4.4 爆炸和破片

爆炸使用距离衰减和装甲遮挡透过率：距离越远 `blast` 越低；中间装甲层按 `1 / (1 + plateMm × resistance / 20)` 衰减。结果是无量纲伤害代理，不是压力、冲量、热量或结构应力。

默认生成 192 条固定种子、等权重、球面方向破片射线。每条权重为 `1 / fragmentSamples`，总权重应为 1。破片会被装甲预算消耗，首个命中模块吸收该样本。界面最多显示约 96 条路径，显示数量不会改变计算结果。

当前没有真实爆炸物、密闭舱压力反射、开口传播、火灾、进水、结构断裂、碎片刚体碰撞或 CFD。

## 5. 已完成验证

`solver_verification.json` 当前记录：

- 10/10 NUnit 断言通过。
- 19/19 `RangeChecks` 通过。
- 固定种子重复性、厚板哑弹停止、接触引信、延迟引信、零阻力、板层爆炸遮挡、破片权重归一、事件单调和伤害边界通过。
- 200 次串行求解样本：中位数约 0.044 ms，P95 约 0.099 ms。

上述耗时包括完整诊断报告分配，只能说明求解器规模很小，不能代表 Unity 渲染帧率、多人舰队性能或 GPU 性能。

## 6. 截图所示编译问题

曾出现 Safe Mode 错误：

```text
Assets/Editor/DamageRangeOpenOnLoad.cs(22,18):
error CS0117: 'Menu' does not contain a definition for 'ExecuteMenuItem'
```

原因是 Unity 2022 中应使用 `EditorApplication.ExecuteMenuItem`。该错误已修正，并已同步到资产仓库；`ShipAssetValidator.cs(348,17)` 的 CS0162 是已有不可达代码警告，不是 Safe Mode 的阻塞错误。

修复后应关闭并重新打开项目，或等待 Unity 重新导入脚本；确认 Console 的红色错误计数归零后再退出 Safe Mode。若仍有红色错误，先处理第一条错误，不要被黄色警告分散注意力。

## 7. 验收状态（P0 · 2026-09-20 更新）

| 项 | 结果 |
|---|---|
| `DamageRangeBuilder.BuildBatch` | **Succeeded errors=0** |
| 独立播放器 | `queen_mary_v3/damage_range/unity_acceptance/player/QueenMaryDamageRange.exe` |
| baseline / benchmark | `unity_acceptance/baseline.json` · `solver_benchmark.txt`（200 发串行，mean≈0.285 ms，非 FPS） |
| Unity Test Runner EditMode | **13/13 Passed**（DamageRangeTests 10 + PenetrationDataTests 3）见 `editmode_damagerange.xml` |
| 源码入库 | 资产仓库 master；推送 `wi-naval-game` |

仍属**人工可选**：编辑器里目视 `DamageRange.unity` 的 Play UI（参数面板、回放、导出 `last_shot.json`）。批处理已证明场景可生成、求解器可测、播放器可构建。

**P1 未做**：`ShipHitResolver` ↔ 试验场模块 ID 映射与主契约数据迁移。

## 8. 已知问题和风险

1. 本次损伤试验场相关文件目前是 Git 未跟踪文件，需要审阅后再提交。
2. `DamageRangeOpenOnLoad.cs` 会在一次编辑器会话的第一次脚本加载时尝试打开试验场；若以后不希望自动切场景，可以删除该文件，只保留菜单入口。
3. 场景中的模块和板层是通用剖段，不是按 Queen Mary 的实际舱段图逐舱还原。
4. 视觉板厚最低 40 mm 与计算板厚分离；不要从视觉尺寸反推装甲数据。
5. 破片目前是射线样本，不是实体碎片；“破片路径”是解释图层，不是 PhysX 结果。
6. `RangeReport.historicallyCertified` 固定为 `false`，后续取得可靠史料后才能逐层替换估算参数。
7. 不要把损伤试验场的 `RangeSolver` 直接替换主战斗的 `ShipHitResolver`；两者目前是不同层级的实验契约。

## 9. 下一步工作顺序

### P0：完成 Unity 实机验收

1. 在 Unity 编辑器中打开 `DamageRange.unity`。
2. 检查 Scene 视图是否生成 `Experimental_Hull_Section`。
3. 点击 Play，确认 UI、FIRE、Pause、Step event、Restart、剖视开关和 JSON 导出。
4. 在 Test Runner 运行 `DamageRangeTests`，保存新的 XML。
5. 若出现错误，记录 Console 中第一条红色错误和脚本行号，再修复。

### P1：建立主战斗接入边界

1. 定义从 `ShipHitResolver` 到损伤报告的统一 `ImpactEventId`。
2. 将模块 ID 从实验名映射到 `ShipSystemsState`，保留未知模块的拒绝和日志。
3. 把装甲层、舱室和设备中心从硬编码迁移到 `Resources/Ships/HMS_Queen_Mary_1913/` 数据文件。
4. 保持表现回放只读报告，不能由 UI 修改战斗结算。

### P2：提高可信计算

1. 引入弹丸质量、口径、弹型、撞击速度、撞击角和材料类别。
2. 将速度平方代理替换为可追溯的穿深曲线或数据表，并为每条数据保留来源。
3. 增加跳弹、帽体/弹尖、板材倾角、穿板时间和破口尺寸事件。
4. 将爆炸从无量纲 proxy 分成装甲内爆、舱外爆、开口传播和压力舱段模型。
5. 为破片引入质量、速度、方向分布和命中面积，保持可控的样本预算。

### P3：清楚剖视和舰船资产接入

1. 用 Queen Mary v4 的内部 FBX 和真实舱室代理替换通用剖段展示。
2. 保持外层装甲、炮塔、弹药库、锅炉房、机舱和操舵设备的独立 ID。
3. 增加模块显隐、按命中层定位、损伤热力图和爆炸事件定位。
4. 再做 15 舰同屏和剖视 LOD 压测；不要把试验场的高可见性几何直接当作舰队运行时资产。

## 10. 给下一位开发者

先打开 `DamageRange.unity` 验证入口和画面，再看 `DamageRangeModel.cs` 的报告结构。修改求解器时先补一个纯测试，再改视图。修改视图时不要让 UI、材质、破片路径写回主战斗状态。所有历史数据都要标注来源或 `estimate`；没有资料的地方保留估算，不要用灰盒视觉尺寸冒充史实参数。

当前最有价值的下一份证据是 Unity Test Runner 的新 XML、一次 Play 模式截图或录屏，以及一份导出的 `last_shot.json`。拿到这三项后，才能把本试验场从“源码和求解器完成”推进为“Unity 实机可复验”。
